<h1>All Tags</h1>
<% if flash[:success] %>
	<%= flash[:success] %>
<% end %>
<ul id='tags'>
		<% @tags.each do |tag| %>
			<li>
				<%= link_to tag.name, tag_path(tag) %>
			</li>

		<% end %>
</ul>

